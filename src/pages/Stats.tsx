import { de } from '../i18n/de';
import { PagePlaceholder } from './PagePlaceholder';

export function Stats() {
  return <PagePlaceholder title={de.pages.stats.title} description={de.pages.stats.placeholder} />;
}
