import { de } from '../i18n/de';
import { PagePlaceholder } from './PagePlaceholder';

export function Dashboard() {
  return <PagePlaceholder title={de.pages.dashboard.title} description={de.pages.dashboard.placeholder} />;
}
