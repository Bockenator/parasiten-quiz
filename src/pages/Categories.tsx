import { de } from '../i18n/de';
import { PagePlaceholder } from './PagePlaceholder';

export function Categories() {
  return <PagePlaceholder title={de.pages.categories.title} description={de.pages.categories.placeholder} />;
}
