import { de } from '../i18n/de';
import { PagePlaceholder } from './PagePlaceholder';

export function Quiz() {
  return <PagePlaceholder title={de.pages.quiz.title} description={de.pages.quiz.placeholder} />;
}
