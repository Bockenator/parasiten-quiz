import { de } from '../i18n/de';
import { PagePlaceholder } from './PagePlaceholder';

export function Result() {
  return <PagePlaceholder title={de.pages.result.title} description={de.pages.result.placeholder} />;
}
