import { de } from '../i18n/de';
import { PagePlaceholder } from './PagePlaceholder';

export function Settings() {
  return <PagePlaceholder title={de.pages.settings.title} description={de.pages.settings.placeholder} />;
}
